# IMPORTANT: Environment Variables Configuration
# Copy this file to .env and add your actual API keys

# =============================================================================
# LLM API KEYS (REQUIRED)
# =============================================================================

# Google Gemini API (Primary)
GOOGLE_API_KEY=your_gemini_api_key_here

# Anthropic Claude API (Fallback - Optional)
ANTHROPIC_API_KEY=your_claude_api_key_here

# OpenAI GPT-4 (Fallback - Optional)
OPENAI_API_KEY=your_openai_api_key_here

# LLM Configuration
LLM_PRIMARY_PROVIDER=gemini  # gemini, claude, or openai
LLM_PRIMARY_MODEL=gemini-1.5-pro  # or gemini-1.5-flash for faster/cheaper
LLM_FALLBACK_MODEL=claude-3-5-sonnet-20241022
LLM_MAX_TOKENS=8192
LLM_TEMPERATURE=0.1
LLM_TIMEOUT=60

# =============================================================================
# INFRASTRUCTURE (REQUIRED FOR PRODUCTION)
# =============================================================================

# Redis (Event Bus)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# PostgreSQL (Data Storage)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=seraphs
POSTGRES_USER=seraphs_user
POSTGRES_PASSWORD=your_secure_password_here

# IPFS (Content Storage)
IPFS_HOST=127.0.0.1
IPFS_PORT=5001
# OR use Pinata for production:
PINATA_API_KEY=your_pinata_key_here
PINATA_SECRET_KEY=your_pinata_secret_here

# =============================================================================
# BLOCKCHAIN (OPTIONAL BUT RECOMMENDED)
# =============================================================================

# Cardano Blockfrost API
BLOCKFROST_API_KEY=your_blockfrost_key_here
BLOCKFROST_NETWORK=mainnet  # or preprod for testnet

# =============================================================================
# EXTERNAL DATA SOURCES (OPTIONAL)
# =============================================================================

# For Agent 7 (Oracle API)
OFAC_API_KEY=optional_if_needed
FATF_API_KEY=optional_if_needed

# =============================================================================
# MONITORING & ALERTS (OPTIONAL)
# =============================================================================

# Sentry Error Tracking
SENTRY_DSN=your_sentry_dsn_here

# Slack Alerts
SLACK_WEBHOOK_URL=your_slack_webhook_here

# Email Alerts
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_email_app_password
ALERT_EMAIL=compliance-team@yourcompany.com

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================

# Environment
ENVIRONMENT=development  # development, staging, production

# Logging
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR

# Scheduling
ENABLE_SCHEDULER=true
SCHEDULER_TIMEZONE=Asia/Kolkata

# Security
SECRET_KEY=generate_a_random_secret_key_here
JWT_SECRET=generate_another_random_secret_here

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================

# 1. Copy this file:
#    cp .env.example .env

# 2. Add your Gemini API key:
#    GOOGLE_API_KEY=AIzaSy... (your actual key)

# 3. For local development, minimal setup:
#    - Just GOOGLE_API_KEY is required
#    - Redis/PostgreSQL/IPFS optional (will use simulated mode)

# 4. For production deployment:
#    - All infrastructure keys required
#    - Set ENVIRONMENT=production
#    - Enable monitoring and alerts

# 5. SECURITY WARNING:
#    - NEVER commit .env file to git
#    - Keep API keys secret
#    - Use different keys for dev/staging/production
